#include <string.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    int len, numint;
    char input[20], numchar[3], output[20];
    scanf("%s", input);
    len = strlen(input);
    if (
        input[len - 3] >= '0' &&
        input[len - 3] <= '9' &&
        input[len - 2] >= '0' &&
        input[len - 2] <= '9' &&
        input[len - 1] >= '0' &&
        input[len - 1] <= '9')
    {
        printf("this is a number ");

        numchar[0] = input[len - 3];
        numchar[1] = input[len - 2];
        numchar[2] = input[len - 1];
        numint = atoi(numchar);
        printf("%d\n", numint);
    }*/
    return sizeof(int);
}